# Supabase Configuration
# DO NOT COMMIT ACTUAL VALUES - Use these keys to set your environment variables

# Public keys (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Private keys (server-side only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Application Setup (for migrations and bootstrap)
APP_SETUP_TOKEN="please-change-me"
APP_SETUP_LOCKED="true"

# Database Connection (for migrations, optional)
# DATABASE_URL="postgres://user:pass@host:5432/db"
# SUPABASE_DB_URL="postgres://user:pass@host:5432/db"

# Email Service (optional for invitations)
# RESEND_API_KEY=your_resend_api_key

# Project Reference (for documentation)
# SUPABASE_PROJECT_ID=your_project_id
